$("#quantity-buttons-<%= @product.id %>").empty()
$("#quantity-buttons-<%= @product.id %>").html("<%= escape_javascript render(:partial => 'quantity_buttons', locals: { cart_product: @cart_product, product: @product }) %>")