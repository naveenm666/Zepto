<h1><%= @subcategory.name %></h1>

<h2>Products</h2>
<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, category_subcategory_product_path(@category, @subcategory, product) %></li>
  <% end %>
</ul>
